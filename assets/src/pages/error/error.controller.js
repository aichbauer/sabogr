angular
  .module('pages.error')
  .controller('ErrorCtrl', ErrorController);

/**
 * @ngdoc controller
 * @name pages.landing:LandingCtrl
 *
 * @requires $scope
 *
 * @description
 * LandingCtrl for the landing page
 */
ErrorController.$inject = [
  '$scope'
];

function ErrorController($scope) {
}
