angular
  .module('pages.footer')
  .controller('FooterCtrl', FooterController);

/**
 * @ngdoc controller
 * @name pages.footer:FooterCtrl
 *
 * @requires $scope
 *
 * @description
 * FooterCtrl for the footer
 */
FooterController.$inject = [
  '$scope'
];

function FooterController($scope) {
}
